{
  "name": "clicker-game",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cap:init": "cap init ClickerGame com.clickergame.app --web-dir dist",
    "cap:sync": "cap sync",
    "cap:sync:android": "cap sync android",
    "cap:sync:ios": "cap sync ios",
    "cap:open:android": "cap open android",
    "cap:open:ios": "cap open ios",
    "android": "npm run build && npm run cap:sync:android && npm run cap:open:android",
    "ios": "npm run build && npm run cap:sync:ios && npm run cap:open:ios"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@capacitor/android": "^8.4.1",
    "@capacitor/cli": "^8.4.1",
    "@capacitor/core": "^8.4.1",
    "@capacitor/ios": "^8.4.1",
    "decimal.js": "^10.6.0",
    "phaser": "^4.2.1"
  },
  "devDependencies": {
    "vite": "^8.1.4"
  }
}
